# Agent notes — terminal-gui-prompts

Project facts for agents. Workstation/env facts live only in `$CODE_ROOT/MEMORIES.md`.

- Canonical GitHub home: `openshellorg/terminal-gui-prompts` (not amdphreak or Dev-Centr)
- Antora component: `terminal-gui-prompts` (registered in `open-shell-org` remote + local playbooks)
- Core binary is D (`core/`, `tgp`); Node package is the primary JS/TS adapter
